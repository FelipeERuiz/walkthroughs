La password es:
```
natas0
```
El objetivo de este nivel es conectarse al usuario `natas0` mediante http al dominio `http://natas0.overthewire.org`.
Va a salir un `popup` donde hay que ingresar la contraseña
`natas0`

